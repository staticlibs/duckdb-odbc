name: Test
on:
  issue_comment:
    types: 
      - created

jobs:
  odbc-vendoring-dispatch:
    name: ODBC vendoring dispatch 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: PR comment HEAD branch
        id: pr_comment_head_branch
        uses: ./.github/actions/pr_comment_head_branch

      - name: Merge
        if: github.repository == 'staticlibs/duckdb-odbc'
        shell: bash
        run: |
            # Store the PAT in a file that can be accessed by the
            # GitHub CLI.
            echo "steps.pr_comment_head_branch.outputs.head_branch: ${{ steps.pr_comment_head_branch.outputs.head_branch }}"
            echo "${{ secrets.GH_TOKEN }}" > token.txt
            # Authorize GitHub CLI for the current repository and
            # create a pull-requests containing the updates.
            #gh auth login --with-token < token.txt
            #gh pr merge vendoring \
            # --subject "${{ github.event.comment.body }}" \
            # --body "" \
            # --rebase
            #git push --delete origin vendoring
            #git branch vendoring
            #git push -f origin vendoring
