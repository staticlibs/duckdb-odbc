name: Test
on:
  issue_comment:

jobs:
  odbc-linux-amd64:
    name: ODBC Linux (amd64)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ inputs.git_ref }}

      - name: Merge
        if: github.repository == 'staticlibs/duckdb-odbc'
        shell: bash
        run: |
            # Store the PAT in a file that can be accessed by the
            # GitHub CLI.
            echo "github.event.pull_request.title: ${{ github.head_ref }}"
            echo "github.event.pull_request.head.ref: ${{ github.event.pull_request.head.ref }}"
            #echo "${{ secrets.GH_TOKEN }}" > token.txt
            # Authorize GitHub CLI for the current repository and
            # create a pull-requests containing the updates.
            #gh auth login --with-token < token.txt
            #gh pr merge vendoring \
            # --subject "${{ github.event.comment.body }}" \
            # --body "" \
            # --rebase
